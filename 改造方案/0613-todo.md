任务：只修 Luma 的拖拽排序提交语义。不要改 hitTest、菜单、本地化、Layout 性能、文件夹切页、wiggle 动画。

允许读取和修改：
- Sources/MacOSLauncher/Features/Launcher/State/LauncherStore.swift
- Sources/MacOSLauncher/Features/Launcher/UI/LauncherViews.swift
- 与 LauncherTileViewDelegate / LauncherTileView 拖拽相关的同文件代码
- 相关测试文件

禁止：
- 不要全仓库扫描；
- 不要改 LauncherRootView.hitTest；
- 不要改 LauncherPagerView.hitTest 主路径；
- 不要改 HeaderButton.mouseDown；
- 不要重写 Pager；
- 不要处理 UI 文案和菜单位置；
- 不要处理背景、壁纸、启动动画。

目标：
1. 默认状态下，用户长按 App 并拖动排序，鼠标松开后立即提交并保存，不需要点击对钩。
2. 只有用户主动点击顶部编辑按钮进入整理模式后，拖动排序才暂存到内存，点击对钩后统一保存。
3. 快速拖拽结束后不要继续停留在编辑/抖动状态。
4. 手动整理模式下，拖完一个 App 后仍保持编辑状态，直到点击对钩。

实现要求：

一、增加排序会话类型

在 LauncherStore 中增加：

enum ReorderSessionKind {
    case none
    case quickDrag
    case manualEdit
}

enum DragCommitPolicy {
    case autoCommit
    case manualCommit
}

增加状态：

private(set) var reorderSessionKind: ReorderSessionKind = .none
private var manualEditOriginalTileOrder: [String]?
private var activeDragCommitPolicy: DragCommitPolicy = .autoCommit

增加只读属性：

var isInManualEditMode: Bool {
    reorderSessionKind == .manualEdit
}

var hasPendingDragPreview: Bool {
    dragPreviewChanged
}

二、拆分手动编辑

新增：

func beginManualEditing()
func commitManualEditing()
func cancelManualEditing()

toggleEditing 改为：
- 如果当前是 manualEdit，则 commitManualEditing()
- 否则 beginManualEditing()

手动编辑逻辑：
- beginManualEditing 保存 manualEditOriginalTileOrder；
- commitManualEditing 保存 preferences 并退出编辑；
- cancelManualEditing 恢复 manualEditOriginalTileOrder 并退出编辑。

三、修改 beginDraggingTile

把 beginDraggingTile(_:) 改成：

func beginDraggingTile(_ tileID: String, commitPolicy: DragCommitPolicy)

规则：
- autoCommit：reorderSessionKind = .quickDrag，isEditing 可临时设为 true，拖拽结束后退出；
- manualCommit：reorderSessionKind = .manualEdit，拖拽结束后仍保持编辑；
- 如果 sortMode 不是 custom，先把 orderedTiles 写入 tileOrder，并切到 custom。

四、修改 endDraggingTile(commit:)

规则：
- autoCommit：
  - commit=true 且 dragPreviewChanged=true：savePreferences()
  - commit=false：恢复 tileOrderBeforeDrag
  - 结束后 reorderSessionKind=.none，isEditing=false
- manualCommit：
  - commit=true：只保留内存顺序，不保存，不退出编辑
  - commit=false：只回滚本次拖拽
  - 仍保持 reorderSessionKind=.manualEdit，isEditing=true
  - 最终保存由 commitManualEditing 负责

五、拖拽结束必须传 NSDragOperation

把 LauncherTileViewDelegate 的：

func tileViewDidEndDragging(_ view: LauncherTileView)

改为：

func tileViewDidEndDragging(_ view: LauncherTileView, operation: NSDragOperation)

在 draggingSession endedAt 中传 operation。

Pager 结束拖拽时：

let shouldCommit =
    currentDragCommitted ||
    (operation.contains(.move) && store.hasPendingDragPreview)

store.endDraggingTile(commit: shouldCommit)

不要因为 performDropWith 没有触发就默认回滚。

六、保留 App-to-App 创建文件夹，但不要抢普通排序

普通拖拽排序应默认 reorder。App 拖到 App 中心区域才 createFolder；拖到边缘或普通 hover 目标时只 reorder。

本轮可先采用最小规则：
- 默认 app -> app 为 reorder；
- 只在明确 createFolder intent 时创建文件夹；
- 不要让普通排序 drop 进入 createFolder 分支。

日志要求：
- drag.begin 记录 commitPolicy
- drag.end 记录 commit、previewChanged、commitPolicy、sessionKind
- manualEdit.begin / manualEdit.commit / manualEdit.cancel

验收：
1. 不点编辑按钮，长按拖动 App，松手后顺序保持；
2. 关闭重开后顺序仍保持；
3. 日志中 quick drag 为 commitPolicy=autoCommit，drag.end commit=true；
4. 点击编辑按钮进入整理模式，拖动多个 App 后仍保持编辑状态；
5. 点击对钩后保存并退出；
6. 手动整理模式拖动时不立即 savePreferences；
7. swift build 通过；
8. 只跑相关测试，不要跑无关全量测试，除非最后确认。

---
任务：只修 Luma 的默认筛选模式、筛选切页动画、创建文件夹后切页动画。不要改拖拽排序提交、hitTest、菜单位置、wiggle、Layout 性能。

允许读取和修改：
- Sources/MacOSLauncher/Features/Launcher/State/LauncherStore.swift
- Sources/MacOSLauncher/Features/Launcher/UI/LauncherViews.swift
- Sources/MacOSLauncher/Shared/Models/Models.swift
- 相关测试文件

禁止：
- 不要全仓库扫描；
- 不要改 LauncherRootView.hitTest；
- 不要改 HeaderButton.mouseDown；
- 不要改 App 名称解析；
- 不要改拖拽排序状态机；
- 不要重写 Pager。

目标：
1. 默认显示“可见应用”，不是“全部应用”。
2. 切换 可见应用 / 全部应用 / 已隐藏应用 时，如果当前不在第一页，应动画回到第一页。
3. 创建文件夹后，应动画切换到新文件夹所在页面。
4. 不要用 .content(animated:true) 粗暴刷新这些场景。

一、默认筛选模式

确认：
- LauncherStore 默认 appFilterMode = .visibleOnly
- LauncherPreferences 默认 appFilterMode = .visibleOnly
- 旧 preferences 没有 appFilterMode 字段时默认 visibleOnly
- Hide App 后如果当前是 .all，自动切回 .visibleOnly

二、增加 StoreChange

在 LauncherStoreChange 增加：

case filterModeChanged(previousPageIndex: Int)
case folderCreated(folderID: String, previousPageIndex: Int, targetPageIndex: Int)

三、修改 setAppFilterMode

规则：
- 记录 previousPageIndex
- 设置 appFilterMode
- pageIndex = 0
- pageDragRawOffset = 0
- pageDragOffset = 0
- savePreferences()
- onChange?(.filterModeChanged(previousPageIndex: previousPageIndex))

四、RootView 处理 filterModeChanged

新增：

case let .filterModeChanged(previousPageIndex):
    pager.reloadForFilterModeChange(from: previousPageIndex, to: 0)
    updatePageDots()
    updateHeader()
    updateStatus()

五、Pager 增加 reloadForFilterModeChange

逻辑：
- performReload(animated:false, refreshReplicas:false, retargetRunningAnimations:false)
- 先把 contentView 放到 previousPageIndex 对应位置
- 如果 previousPageIndex != 0，则 setPage(index:0, animated:true)
- 如果已经在第一页，则 setPage(index:0, animated:false)
- 延迟刷新 edge replicas

六、创建文件夹后切到文件夹所在页

修改 createFolder：
- 记录 previousPageIndex
- 创建 folder，更新 folders/tileOrder
- reconcileAfterAppScan()
- 计算 folder.tileID 在 visibleTiles 中的 index
- targetPageIndex = index / gridLayout.itemsPerPage
- pageIndex = targetPageIndex
- savePreferences()
- onChange?(.folderCreated(folderID: folder.id, previousPageIndex: previousPageIndex, targetPageIndex: targetPageIndex))

RootView 处理：

case let .folderCreated(_, previousPageIndex, targetPageIndex):
    pager.reloadForFolderCreation(from: previousPageIndex, to: targetPageIndex)
    updatePageDots()
    updateHeader()
    updateStatus()

Pager 增加 reloadForFolderCreation：
- performReload(animated:false, refreshReplicas:false, retargetRunningAnimations:false)
- 从 previousPageIndex 动画滑到 targetPageIndex
- 同页则直接刷新
- 延迟刷新 edge replicas

日志要求：
- filterMode.changed previousPageIndex targetPageIndex
- folder.created previousPageIndex targetPageIndex
- pager.reloadForFilterModeChange
- pager.reloadForFolderCreation

验收：
1. 首次启动默认是可见应用；
2. 在第 2/3 页切换筛选模式，会动画回到第 1 页；
3. 在第 1 页切换筛选模式，不做多余滑动；
4. 新建文件夹后动画切到新文件夹所在页；
5. App-to-App 创建文件夹后也切到新文件夹所在页；
6. pageDots 同步；
7. swift build 通过；
8. 只跑相关测试。

---

任务：只把 Luma 编辑态从右上角拖动图标改成 iOS / Launchpad 风格图标抖动。不要改拖拽排序提交、筛选切页、文件夹切页、hitTest、菜单位置、本地化。

允许读取和修改：
- Sources/MacOSLauncher/Features/Launcher/UI/LauncherViews.swift
- LauncherTileView 所在文件
- Sources/MacOSLauncher/Features/Launcher/State/LauncherStore.swift，仅可增加 shouldShowJiggle 只读属性
- Sources/MacOSLauncher/Shared/Localization/LumaLocalization.swift，仅可补 tooltip 文案
- 相关测试文件

禁止：
- 不要全仓库扫描；
- 不要重写 Pager；
- 不要改 hitTest；
- 不要改 HeaderButton.mouseDown；
- 不要改拖拽排序提交逻辑；
- 不要用大量 Timer 或每帧手动改 frame。

目标：
1. 点击顶部编辑按钮后，图标像 iOS / Launchpad 一样轻微抖动。
2. 不再显示右上角 line.3.horizontal 拖动图标。
3. 拖动中的 Tile 暂停抖动。
4. 退出编辑后停止抖动。
5. 默认快速长按拖动结束后不要长期抖动。
6. 支持 Reduce Motion。

一、Store 暴露 shouldShowJiggle

在 LauncherStore 增加：

var shouldShowJiggle: Bool {
    reorderSessionKind == .manualEdit
}

二、Pager 传递 jiggle 状态

在 updateEditingState 或 Tile 更新处：

view.setEditing(
    store.shouldShowJiggle,
    dragged: store.draggedTileID == view.tileID
)

这里的 editing 参数语义改为 showJiggle，不要再表示所有拖拽状态。

三、删除或隐藏 editBadge

在 LauncherTileView：
- 删除 editBadge 属性、初始化、addSubview、layout；
- 或最小改动：永远 editBadge.isHidden = true；
- 不要再显示 line.3.horizontal。

优先删除无用代码。

四、实现 wiggle

在 LauncherTileView 增加：

private var shouldReduceMotion: Bool {
    NSWorkspace.shared.accessibilityDisplayShouldReduceMotion
}

private func updateWiggleAnimation() {
    if shouldReduceMotion {
        stopWiggle()
        return
    }

    if isEditing && !isDraggingTile {
        startWiggleIfNeeded()
    } else {
        stopWiggle()
    }
}

private func startWiggleIfNeeded() {
    wantsLayer = true
    guard layer?.animation(forKey: "luma.wiggle.rotation") == nil else { return }

    let seed = abs(tileID.hashValue)
    let phase = CFTimeInterval(seed % 100) / 100.0 * 0.12
    let angle = CGFloat(0.018 + Double(seed % 6) / 1000.0)

    let rotation = CAKeyframeAnimation(keyPath: "transform.rotation.z")
    rotation.values = [-angle, angle, -angle * 0.7]
    rotation.duration = 0.18
    rotation.beginTime = CACurrentMediaTime() + phase
    rotation.repeatCount = .infinity
    rotation.isRemovedOnCompletion = false
    rotation.timingFunction = CAMediaTimingFunction(name: .easeInEaseOut)

    let translation = CAKeyframeAnimation(keyPath: "transform.translation.y")
    translation.values = [0, -1.0, 0.6, 0]
    translation.duration = 0.24
    translation.beginTime = CACurrentMediaTime() + phase / 2
    translation.repeatCount = .infinity
    translation.isRemovedOnCompletion = false
    translation.timingFunction = CAMediaTimingFunction(name: .easeInEaseOut)

    layer?.add(rotation, forKey: "luma.wiggle.rotation")
    layer?.add(translation, forKey: "luma.wiggle.position")
}

private func stopWiggle() {
    layer?.removeAnimation(forKey: "luma.wiggle.rotation")
    layer?.removeAnimation(forKey: "luma.wiggle.position")
    layer?.transform = CATransform3DIdentity
}

调用位置：
- update(tile:metrics:isEditing:) 末尾
- setEditing(_:dragged:) 末尾
- mouseDragged 开始拖拽后
- draggingSession endedAt 后
- 退出编辑时

五、Header 编辑按钮

非编辑态：
- symbol 建议 "square.grid.3x3"
- tooltip “整理应用”

手动编辑态：
- symbol "checkmark"
- tooltip “完成排序”

L10n 可新增：
- organizeApps: 整理应用 / Organize Apps
- doneSorting: 完成排序 / Done Sorting

验收：
1. 点击编辑按钮后，所有可见 Tile 轻微抖动；
2. 不再出现右上角三横线图标；
3. 拖动中的 Tile 暂停抖动；
4. 拖完后 manual edit 仍抖动；
5. 点击对钩后全部停止；
6. 默认快速拖拽结束后不保留抖动；
7. Reduce Motion 开启时不添加 wiggle 动画；
8. swift build 通过。

---

任务：减少测试代码和 Codex 消耗。不要修改生产逻辑。

允许修改：
- Tests/LumaTests/...

禁止修改：
- Sources/MacOSLauncher/Features/Launcher/UI/LauncherViews.swift
- Sources/MacOSLauncher/Features/Launcher/State/LauncherStore.swift
- 任何生产代码，除非编译必须

目标：
1. 删除或暂停复杂 AppKit UI 交互测试；
2. 保留纯逻辑测试；
3. 后续默认不跑 swift test，只跑 swift build。

删除或暂停：
- 真实 NSWindow hitTest 测试；
- LauncherInteractionTests 中依赖窗口、拖拽、菜单弹出、动画的测试；
- Pager 动画测试；
- Header 菜单位置测试；
- Tile wiggle 动画测试。

保留：
- LauncherStore 排序逻辑测试；
- hiddenAppIDs / appFilterMode 逻辑测试；
- createFolder / addAppToFolder 逻辑测试；
- ApplicationDisplayNameResolver 测试；
- L10n 测试；
- TileOrderMover 测试。

如果删除测试文件导致 Package 测试 target 编译失败，请同步清理引用。

验收：
- swift build 通过；
- 不要求 swift test；
- Tests 目录明显变小；
- 生产代码不发生行为变化。