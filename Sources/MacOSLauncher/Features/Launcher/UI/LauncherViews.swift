/// Launcher UI types are split across dedicated files.
///
/// `LauncherRootView.swift` hosts the root container and header wiring.
/// `LauncherPagerView.swift` hosts paging, looping replicas, and drag forwarding.
/// `LauncherTileView.swift` hosts interactive tile rendering and drag behavior.
/// `LauncherReplicaViews.swift` hosts non-interactive edge replica views.
